{% if navList %}
{% else %}
{% set navList = [
  {
    link: 'О нас',
    ref: 'welcome.html'
  },
  {
    link: 'Партнерская программа',
    ref: 'single-page.html'
  },
  {
    link: 'Контакты',
    ref: 'footer',
    ancor: true,
  },
  ] %}
{% endif %}

<nav class="nav {{navMod}}">
  <ul class="nav__list">
    {% for item in navList %}
      <li class="nav__item">
        <a href="{% if item.ancor %}{{item.ref}}{% else %}../../{{item.ref}}{% endif %}" class="nav__link{% if item.ancor %} js-ancor-link{% endif %}">
          {{item.link}}
        </a>
      </li>
    {% endfor %}
  </ul>
</nav>